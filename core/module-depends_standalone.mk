# standalone makefile to generate file containing all project mods needed by current mods.
# This help the creation of the main depends makefile at the app level.

include $(ABSROOT)/core/common.mk
include $(ABSROOT)/core/module-depends.mk