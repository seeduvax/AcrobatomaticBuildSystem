# This library permit to test an archive with a different name than the .so generated (case of eigen, jsoncpp, ...)
# This library test a named with dash too.

APPNAME=testlib-dashed
VERSION=1.0.1
COMPANY=eduvax
VABS:=99.99.99
ABS_REPO:=file://$(PRJROOT)/../repository http://www.eduvax.net/dist file:///home/httpd/www/dist
DISTREPO:=file://$(PRJROOT)/../repository
ABSWS:=$(PRJROOT)/../absws

WORKSPACE_IS_TAG:=true
NO_VINFO=true
PUBLISH_TO_APP_LEVEL=true